/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.spoonacular.com'], // Allow images from the spoonacular domain
      },
    };


export default nextConfig;
